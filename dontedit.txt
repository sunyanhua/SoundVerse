❯ 音频工坊栏目做以下调整： 
  1.增加节目列表，默认打开是已经上传的节目列表，包括节目名称（即为音频文件名称）、节目时长、在线播放、节目裁切状态（进行中、已完成）、语弹数量、节目管理（重新裁切/删除，操作将清空该节目已经生成的语弹文件及数据），列表右上方有“上传新节目”按钮，点击后，是当前的上传界面；
  2.上传界面中，节目上传完成后，即先进入节目数据库，然后后端异步进程处理该节目的语弹裁切，并更新该节目裁切状态、语弹数量。
  3.语弹列表中加上每条语弹所属节目字段。
  4.新增节目列表样式请与语弹列表UI统一。